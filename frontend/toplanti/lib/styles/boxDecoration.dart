import 'package:flutter/material.dart';

const BoxDecoration standardContainerDecoration = BoxDecoration(
  color: Color(0xFF1E293B),
  borderRadius: BorderRadius.all(Radius.circular(16)),
  boxShadow: [
    BoxShadow(
      color: Colors.black26,
      blurRadius: 6,
      offset: Offset(2, 2),
    ),
  ],
);
